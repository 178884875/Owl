
/**
 * 将ms转换为秒
 * @param ms 
 * @returns 秒
 */
export const msToSeconds = (ms: number) => {
    return ms / 1000;
}

/**
 * 将ms转换为分钟
 * @param ms 
 * @returns 分钟
 */
export const msToMinutes = (ms: number) => {
    return ms / 1000 / 60;
}

